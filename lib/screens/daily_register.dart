import 'package:flutter/material.dart';

class DailyRegister extends StatelessWidget {
  const DailyRegister({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Daily Register');
  }
}

import 'package:flutter/material.dart';

class MaterialUse extends StatelessWidget {
  const MaterialUse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Materials Used');
  }
}
